# Prometheus Ansible Roles

## Status

status: scaffold

## Purpose

This folder is reserved for Prometheus-specific roles if reusable shared roles are not appropriate.

Prefer shared roles under [[automation/shared/ansible/roles/README]] when behavior is reusable across domains.
