# /mnt/atlas/downloads

## Purpose

This folder documents `/mnt/atlas/downloads` as observed in the live tree.

## Status

Directory exists in the live tree, but it was not reported as an active Atlas NFS mount in the latest read-only validation. qBittorrent downloads remain local at `/opt/torrents/downloads`.
