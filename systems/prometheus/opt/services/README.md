# /opt/services

## Purpose

This folder documents the desired `/opt/services` area for optional app-owned source trees.

## Status

Desired normalized path. Current live app-owned source trees include `/opt/homelable`; they have not been moved.

Validation of this target path is tracked by issue #112.
